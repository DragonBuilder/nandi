version: '3.8'
services:
  http:
    build:
      context: docker/app-local
      dockerfile: Dockerfile
    volumes:
      - ./:/app
    working_dir: /app
    ports:
      - "$PORT:5555"
    env_file: 
      .env